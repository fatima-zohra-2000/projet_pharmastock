<div id="wrapper">
	<!-- Sidebar -->
	<ul class="navbar-nav bg-gradient-primary sidebar sidebar-dark accordion" id="accordionSidebar">

		<!-- Sidebar - Brand -->
		<a class="sidebar-brand d-flex align-items-center justify-content-center" href="{{ path('app_home') }}">
			<div class="sidebar-brand-icon rotate-n-15">
{#				<i class="fas fa-laugh-wink"></i>#}
				<i class="fas fa-laptop-medical"></i>
			</div>
			<div class="sidebar-brand-text mx-3">Pharmastock</div>
		</a>

		<!-- Divider -->
		<hr class="sidebar-divider my-0">

		<!-- Nav Item - Accueil -->
		<li class="nav-item {% if app.request.attributes.get('_route') == 'app_home' %}active{% endif %}">
			<a class="nav-link" href="{{ path('app_home') }}">
				<i class="fas fa-fw fa-home"></i>
				<span>Accueil</span>
			</a>
		</li>

		<!-- Divider -->
		<hr class="sidebar-divider">

		<!-- Heading -->
		<div class="sidebar-heading">
			Interface
		</div>

		<!-- Nav Item - Produits -->
		{% if app.user %}
			{% if is_granted('ROLE_ADMIN') %}
				<li class="nav-item {% if app.request.attributes.get('_route') == 'app_produit_index' %}active{% endif %}">
					<a class="nav-link" href="{{ path('app_produit_index') }}">
						<i class="fas fa-fw fa-shopping-cart"></i>
						<span>Produits</span>
					</a>
				</li>
				<li class="nav-item {% if app.request.attributes.get('_route') == 'app_fournisseur_index' %}active{% endif %}">
					<a class="nav-link" href="{{ path('app_fournisseur_index') }}">
						<i class="fas fa-fw fa-truck"></i>
						<span>Fournisseurs</span>
					</a>
				</li>
				<li class="nav-item {% if app.request.attributes.get('_route') == 'app_categorie_index' %}active{% endif %}">
					<a class="nav-link" href="{{ path('app_categorie_index') }}">
						<i class="fas fa-fw fa-tags"></i>
						<span>Catégories</span>
					</a>
				</li>
			{% endif %}
			<li class="nav-item {% if app.request.attributes.get('_route') == 'app_commande_index' %}active{% endif %}">
				<a class="nav-link" href="{{ path('app_commande_index') }}">
					<i class="fas fa-fw fa-shopping-basket"></i>
					<span>Commandes</span>
				</a>
			</li>
			<li class="nav-item {% if app.request.attributes.get('_route') == 'app_achat_index' %}active{% endif %}">
				<a class="nav-link" href="{{ path('app_achat_index') }}">
					<i class="fas fa-fw fa-cart-plus"></i>
					<span>Achats</span>
				</a>
			</li>
		{% endif %}

		<!-- Divider -->
		<hr class="sidebar-divider">

		<!-- Sidebar Toggler (Sidebar) -->
		<div class="text-center d-none d-md-inline">
			<button class="rounded-circle border-0" id="sidebarToggle"></button>
		</div>

	</ul>
	<!-- End of Sidebar -->

	<!-- Content Wrapper -->
	<div id="content-wrapper" class="d-flex flex-column">

		<!-- Main Content -->
		<div id="content">

			<!-- Topbar -->
			<nav class="navbar navbar-expand navbar-light bg-white topbar mb-5 p-4 static-top shadow">

				<!-- Sidebar Toggle (Topbar) -->
				<button id="sidebarToggleTop" class="btn btn-link d-md-none rounded-circle mr-3">
					<i class="fa fa-bars"></i>
				</button>

				<!-- Topbar Search -->
				<form
						class="d-none d-sm-inline-block form-inline mr-auto ml-md-3 my-2 my-md-0 mw-100 navbar-search">
					<div class="input-group">
						<input type="text" class="form-control bg-light border-0 small" placeholder="Rechercher..."
							   aria-label="Search" aria-describedby="basic-addon2">
						<div class="input-group-append">
							<button class="btn btn-primary" type="button">
								<i class="fas fa-search fa-sm"></i>
							</button>
						</div>
					</div>
				</form>

				<!-- Topbar Navbar -->
				<ul class="navbar-nav ml-auto">

					<!-- Nav Item - User Information -->
					{% if app.user %}
						<li class="nav-item dropdown no-arrow">
							<a class="nav-link dropdown-toggle" href="#" id="userDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
								<span class="mr-2 d-none d-lg-inline text-gray-600 small"><i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>{{ app.user.name }}</span>
							</a>
							<!-- Dropdown - User Information -->
							<div class="dropdown-menu dropdown-menu-right shadow animated--grow-in" aria-labelledby="userDropdown">
								<a class="dropdown-item" href="#">
									<i class="fas fa-user fa-sm fa-fw mr-2 text-gray-400"></i>
									Profil
								</a>
{#								<a class="dropdown-item" href="#">#}
{#									<i class="fas fa-cogs fa-sm fa-fw mr-2 text-gray-400"></i>#}
{#									Paramètres#}
{#								</a>#}
								<div class="dropdown-divider"></div>
{#								<a class="dropdown-item" href="{{ path('app_logout') }}" onclick="event.preventDefault(); document.getElementById('logout-form').submit();">#}
{#									<i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>#}
{#									Déconnexion#}
{#								</a>#}
								<a class="dropdown-item" href="{{ path('app_logout') }}">
									<i class="fas fa-sign-out-alt fa-sm fa-fw mr-2 text-gray-400"></i>
									Déconnexion
								</a>
{#								<form id="logout-form" action="{{ path('app_logout') }}" method="POST" style="display: none;">#}
{#									{{ csrf_token() }}#}
{#								</form>#}
							</div>
						</li>
					{% else %}
						<li class="nav-item">
							<a class="nav-link" href="{{ path('app_login') }}">
								<span class="mr-2 d-none d-lg-inline text-gray-600 small">Connexion</span>
								<i class="fas fa-sign-in-alt"></i>
							</a>
						</li>
					{% endif %}

				</ul>

			</nav>
			<!-- End of Topbar -->

			<!-- Begin Page Content -->
			<div class="container-fluid">

{#				<!-- Page Heading -->#}
{#				<h1 class="h3 mb-4 text-gray-800">{{ page_title }}</h1>#}
			<!-- End of Topbar -->
{#		</div>#}
{#	</div>#}
