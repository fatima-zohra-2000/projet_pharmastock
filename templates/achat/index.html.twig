{% extends 'base.html.twig' %}

{% block title %}Achat index{% endblock %}

{% block body %}
{#    <h1>Achat index</h1>#}

{#    <table class="table">#}
{#        <thead>#}
{#            <tr>#}
{#                <th>Id</th>#}
{#                <th>Num_achat</th>#}
{#                <th>TVA %</th>#}
{#                <th>Mantant_TVA €</th>#}
{#                <th>Total €</th>#}
{#                <th>Date</th>#}
{#                <th>Actions</th>#}
{#            </tr>#}
{#        </thead>#}
{#        <tbody>#}
{#        {% for achat in achats %}#}
{#            <tr>#}
{#                <td>{{ achat.id }}</td>#}
{#                <td>{{ achat.numAchat }}</td>#}
{#                <td>{{ achat.TVA }} %</td>#}
{#                <td>{{ achat.mantantTVA }} €</td>#}
{#                <td>{{ achat.total }} €</td>#}
{#                <td>{{ achat.date ? achat.date|date('d-m-Y à H:i:s') : '' }}</td>#}
{#                <td>#}
{#                    <a href="{{ path('app_achat_show', {'id': achat.id}) }}">show</a>#}
{#                    <a href="{{ path('app_achat_edit', {'id': achat.id}) }}">edit</a>#}
{#                    <a href="{{ path('app_achat_rapport_download', {'id': achat.id}) }}">down</a>#}
{#                </td>#}
{#            </tr>#}
{#        {% else %}#}
{#            <tr>#}
{#                <td colspan="7">no records found</td>#}
{#            </tr>#}
{#        {% endfor %}#}
{#        </tbody>#}
{#    </table>#}

{#    <a href="{{ path('app_achat_new') }}">Create new</a>#}

    <div class="mx-4 my-3">
        <div class="d-sm-flex align-items-center justify-content-between mb-5 mx-5">
            <h1 class="h1 mb-0 text-gray-800">Gestion des achats</h1>
            <span class="d-lg-inline">
            <a href="{{ path('app_achat_new') }}" class="d-none d-sm-inline-block btn btn-sm btn-primary shadow-sm">
                Nouvel achat
            </a>
        </span>
        </div>
        <div class="card shadow mb-5 mx-5">
            <div class="card-body table-responsive">
                <table class="table table-striped table-borderless rounded rounded-3 overflow-hidden">
                    <thead>
                    <tr>
                        <th>Numéro d'achat</th>
                        <th>Total €</th>
                        <th>TVA %</th>
                        <th>Montant TVA €</th>
                        <th>Date</th>
                        <th>Visualiser</th>
                        <th>Modifier</th>
                        <th>Télécharger rapport</th>
                    </tr>
                    </thead>
                    <tbody>
                    {% for achat in achats %}
                        <tr>
                            <td>{{ achat.numAchat }}</td>
                            <td>{{ achat.total }} €</td>
                            <td>{{ achat.TVA }} %</td>
                            <td>{{ achat.mantantTVA }} €</td>
                            <td>{{ achat.date ? achat.date|date('d-m-Y à H:i:s') : '' }}</td>
                            <td class="text-center">
                                <a href="{{ path('app_achat_show', {'id': achat.id}) }}" class="btn btn-sm btn-primary">
                                    <i class="fas fa-eye"></i>
                                </a>
                            </td>
                            <td class="text-center">
                                <a href="{{ path('app_achat_edit', {'id': achat.id}) }}" class="btn btn-sm btn-secondary">
                                    <i class="fas fa-edit"></i>
                                </a>
                            </td>
                            <td class="text-center">
                                <a href="{{ path('app_achat_rapport_download', {'id': achat.id}) }}" class="btn btn-sm btn-success">
                                    <i class="fas fa-download"></i>
                                </a>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="8">Aucun achat trouvé</td>
                        </tr>
                    {% endfor %}
                    </tbody>
                </table>
            </div>
        </div>
    </div>

{% endblock %}
{% block footer %}{% endblock %}
