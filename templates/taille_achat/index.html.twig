{% extends 'base.html.twig' %}

{% block title %}TailleAchat index{% endblock %}

{% block body %}
    <h1>TailleAchat index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Quantite</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for taille_achat in taille_achats %}
            <tr>
                <td>{{ taille_achat.id }}</td>
                <td>{{ taille_achat.quantite }}</td>
                <td>{{ taille_achat.prix }}</td>
                <td>
                    <a href="{{ path('app_taille_achat_show', {'id': taille_achat.id}) }}">show</a>
                    <a href="{{ path('app_taille_achat_edit', {'id': taille_achat.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_taille_achat_new') }}">Create new</a>
{% endblock %}
