<form method="post" action="{{ path('app_taille_achat_delete', {'id': taille_achat.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ taille_achat.id) }}">
    <button class="btn">Delete</button>
</form>
