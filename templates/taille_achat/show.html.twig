{% extends 'base.html.twig' %}

{% block title %}TailleAchat{% endblock %}

{% block body %}
    <h1>TailleAchat</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taille_achat.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ taille_achat.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ taille_achat.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_taille_achat_index') }}">back to list</a>

    <a href="{{ path('app_taille_achat_edit', {'id': taille_achat.id}) }}">edit</a>

    {{ include('taille_achat/_delete_form.html.twig') }}
{% endblock %}
