{% extends 'base.html.twig' %}

{% block title %}Fournisseur{% endblock %}

{% block body %}
<div class="mx-4 my-3">
    <div class="container row m-5">
        <div class="d-flex flex-column align-items-start my-5">
            <div class="d-flex align-items-center justify-content-between col-md-8">
                <h1 class="h1 text-gray-800">{{ fournisseur.nom }}</h1>
                <a href="{{ path('app_fournisseur_index') }}" class="d-none d-sm-inline-block shadow-sm btn btn-sm btn-primary">
                    Retourner à la liste
                </a>
            </div>
            <div class="col-md-8">
                <table class="table table-striped mt-lg-5">
                    <tbody>
                        <tr>
                            <th>Nom</th>
                            <td>{{ fournisseur.nom }}</td>
                        </tr>
                        <tr>
                            <th>Email</th>
                            <td>{{ fournisseur.email }}</td>
                        </tr>
                        <tr>
                            <th>Adresse</th>
                            <td>{{ fournisseur.adresse }}</td>
                        </tr>
                        <tr>
                            <th>Telephone</th>
                            <td>{{ fournisseur.telephone }}</td>
                        </tr>
                    </tbody>
                </table>

                <div class="d-flex mt-5 mb-5">
                    <a href="{{ path('app_fournisseur_edit', {'id': fournisseur.id}) }}" class="mx-2 d-none d-sm-inline-block shadow-sm btn btn-sm btn-secondary">Modifier</a>
                    {{ include('fournisseur/_delete_form.html.twig') }}
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock %}
